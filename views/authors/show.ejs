<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Document</title>
</head>
<body>
  <h1>Lemme see your authors!</h1>
  <p>
    <%= author.firstName%> <%= author.lastName%>'s books:
    <% books.forEach(function(book){ %>
      <p>
        <a href="/authors/<%= author._id%>/books/<%= book.id%>"><%= book.title%></a>

      </p>
    <% })%>
  </p>
  <p>
      <a href="/authors/<%= author._id%>/books/new">Add a new book</a>
  </p>
  <p>
    <a href="/authors/<%= author._id%>/edit">Edit this author</a>
    <form action="/authors/<%= author._id%>?_method=DELETE" method="POST">
      <input type="submit" value="DELETE">
    </form>
  </p>
  <p>
    <a href="/authors">See all authors</a>
  </p>
</body>
</html>